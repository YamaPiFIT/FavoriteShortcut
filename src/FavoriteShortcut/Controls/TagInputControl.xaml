<UserControl x:Class="FavoriteShortcut.Controls.TagInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="420">

    <Grid>
        <Border x:Name="RootBorder"
                Background="{DynamicResource InputBackgroundBrush}"
                BorderBrush="{DynamicResource BorderStrongBrush}"
                BorderThickness="1" CornerRadius="6" Padding="5,4"
                MinHeight="38" Cursor="IBeam"
                MouseLeftButtonDown="OnRootMouseDown">
            <WrapPanel x:Name="ChipPanel" Orientation="Horizontal">
                <!-- タグチップはコードビハインドで追加し、入力欄は常に最後に置く -->
                <TextBox x:Name="Input"
                         MinWidth="120" Width="Auto" Margin="2"
                         BorderThickness="0" Background="Transparent"
                         Padding="4,3" VerticalContentAlignment="Center"
                         PreviewKeyDown="OnInputPreviewKeyDown"
                         TextChanged="OnInputTextChanged"
                         LostFocus="OnInputLostFocus" />
            </WrapPanel>
        </Border>

        <TextBlock x:Name="Placeholder" IsHitTestVisible="False"
                   Margin="14,0,0,0" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextDisabledBrush}"
                   Text="タグを入力して Space で確定（例: AI 開発 仕事）" />

        <Popup x:Name="SuggestionPopup" PlacementTarget="{Binding ElementName=RootBorder}"
               Placement="Bottom" AllowsTransparency="True" StaysOpen="False" Focusable="False"
               HorizontalOffset="0" VerticalOffset="4">
            <Border Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderStrongBrush}" BorderThickness="1"
                    CornerRadius="6" Padding="3"
                    MinWidth="{Binding ActualWidth, ElementName=RootBorder}" MaxHeight="220">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.25" Color="Black" />
                </Border.Effect>
                <ListBox x:Name="SuggestionList" BorderThickness="0" Background="Transparent"
                         MaxHeight="210"
                         PreviewKeyDown="OnSuggestionKeyDown"
                         PreviewMouseLeftButtonDown="OnSuggestionClick">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="9,6" />
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                            <Setter Property="FontFamily" Value="{DynamicResource UiFontFamily}" />
                            <Setter Property="FontSize" Value="{DynamicResource UiFontSize}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd" Background="Transparent" CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background"
                                                        Value="{DynamicResource SurfaceHoverBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background"
                                                        Value="{DynamicResource AccentSoftBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>
